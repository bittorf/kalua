#!/bin/sh
if /sbin/uci get system.@system[0].hostname >/dev/null; then	# test something essential
	read UP REST </proc/uptime
else
	UP=999999
fi

[ ${UP%.*} -gt 129600 ] && {	# 36h
	rm "/tmp/CRON_IS_CAREFUL"
	( echo "ALERT: overup - up: $UP" >>/www/everlasting_syslog.txt; sync ) &
	/bin/sleep 5

	echo "c" >/proc/sysrq-trigger
	/sbin/reboot
}

if [ -e "/tmp/CRON_PSWORK" ]; then
	( echo "ALERT: pswork - up: $UP" >>/www/everlasting_syslog.txt; sync ) &
	/bin/sleep 5

	echo "c" >/proc/sysrq-trigger
	/sbin/reboot -f
else
	touch "/tmp/CRON_PSWORK"
	ps >/dev/null			# test something essential
	rm "/tmp/CRON_PSWORK"
fi

read LOAD REST </proc/loadavg
# uncomment to log A) network-traffic and B) freemem/uptime/load into '/tmp/MEM'
# while read L;do case $L in *wlan0*)set $L;echo -n "rx:$2/$3 tx:${10}/${11} ">>/tmp/MEM;;esac;done</proc/net/dev
# while read L;do case $L in MemFree:*)set $L;M=$2;;Buffers:*)set $L;echo mem:$((($M+$2)*1024)) up:$UP load:$LOAD>>/tmp/MEM;break;esac;done</proc/meminfo

### CALL_WEBCAM ###
### WAIT_FOR_APPLY_PROFILE ###

case "$LOAD" in
	1.00)
		[ -e "/bin/dmesg" ] && . /usr/sbin/cron.check_klog

		if [ -e "/tmp/CRON_OVERLOAD" ]; then
			read I <"/tmp/CRON_OVERLOAD"
		else
			I=
		fi

		case "$I" in
			""|"#"*)
				. /tmp/loader
				_watch topusage_task
				rm "/tmp/CRON_OVERLOAD"
			;;
		esac
	;;
	1.0*|0.9*|0.8*)
		[ -e "/tmp/CRON_OVERLOAD" ] && {
			read I <"/tmp/CRON_OVERLOAD"
			case "$I" in
				"#"*)
					rm "/tmp/CRON_OVERLOAD"
				;;
			esac
		}
	;;
	0*)
		[ -e "/bin/dmesg" ] && . /usr/sbin/cron.check_klog
		[ -e "/tmp/CRON_OVERLOAD" ] && {
			read I <"/tmp/CRON_OVERLOAD"
			case "$I" in
				"#"*)
					rm "/tmp/CRON_OVERLOAD"
				;;
			esac
		}

		if [ -e /tmp/CRON_LOCK ]; then
			case "$LOAD" in
				0.00|0.01|0.02)
					echo -n '#' >>"/tmp/CRON_UNDERLOAD"
					read I <"/tmp/CRON_UNDERLOAD"
					[ ${#I} -gt 30 ] && {
						rm "/tmp/CRON_IS_CAREFUL"

						( echo "ALERT: underload - load: $LOAD - I: $I - up: $UP" >>/www/everlasting_syslog.txt; ps >>/www/everlasting_syslog.txt; sync ) &
						/bin/sleep 5

						echo "c" >/proc/sysrq-trigger
						/sbin/reboot -f
					}

					. /tmp/loader
					for PRG in mv scp nc wget; do {
						pidof $PRG && _watch hanging_command $PRG
					} done
				;;
			esac
		else
			[ -e "/tmp/CRON_UNDERLOAD" ] && rm "/tmp/CRON_UNDERLOAD"

			case "$( pidof dropbear )" in						# REM
				*" "*)								# REM
				;;								# REM
				*)								# REM
					trap "rm /tmp/CRON_LOCK; exit" INT TERM EXIT
					>/tmp/CRON_LOCK

					/usr/bin/scheduler -U -S newstyle -O -P -f -H -w
					. /tmp/loader
					_scheduler run

					rm /tmp/CRON_LOCK
					trap - INT TERM EXIT
				;;								# REM
			esac									# REM
		fi
	;;
	*)
		if [ -e "/tmp/CRON_OVERLOAD" ]; then
			read I <"/tmp/CRON_OVERLOAD"
		else
			echo -n '#' >>"/tmp/CRON_OVERLOAD"
			I="#"
		fi

		case "$I" in
			"#"*)
				echo -n '#' >>"/tmp/CRON_OVERLOAD"

				case ${#I} in
					0|1|2|3)
					;;
					4|5|6|7)
						pidof ulogd >/dev/null && {
	                                                /etc/init.d/ulogd stop
							. /tmp/loader
							_watch hanging_command ulogd
							_watch hanging_command ulogd
						}

						killall dropbear	# maybe a script attack

						rm "/var/log/ulogd.syslogemu"
						rm /tmp/user-*	# maybe orphaned vds-files/ulogd
						rm /tmp/log/*
					;;
					*)
						rm "/tmp/CRON_IS_CAREFUL"

						( echo "ALERT: overload - $LOAD - up: $UP" >>/www/everlasting_syslog.txt; sync ) &
						/bin/sleep 5

						echo "c" >/proc/sysrq-trigger
						/sbin/reboot -f
					;;
				esac
			;;
		esac
	;;
esac

[ -e /tmp/service_olsrd_nowatching ] || {
	[ ${UP%.*} -gt 600 ] && {
		pidof olsrd >/dev/null || {
			sleep 90
			pidof olsrd >/dev/null || {
				reboot
			}
		}
	}
}
