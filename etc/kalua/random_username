_random_username_do ()		# sample-implementation, replaced it using 'mydesign.ipk' with your specific namespace
{
	case "$( _math random_integer 1 3 )" in		# min=1 max=3
		1) echo "alice" ;;
		2) echo "bob" ;;
		*) echo "carol" ;;
	esac
}
