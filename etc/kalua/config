_config_get ()
{
	local SECTION="$1"		# e.g. net
	local SUBSECTION="$2"		# e.g. olsr
	local OPTION="$3"		# e.g. hna4
	
	case $SECTION in
		net)
			case $SUBSECTION in
				olsr)
					case $OPTION in
						hna4)
							nvram get ff_hna4
						;;
					esac
				;;
			esac
		;;
	esac
}
